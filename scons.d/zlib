
# zlib (logs and jabber)

Import('*')

out = conf.CheckLibWithHeader('z', ['zlib.h'], 'C', 'gzopen(NULL, NULL);', 0)
defines['HAVE_ZLIB'] = out
if (out):
	libs.append('z')

Return('out')

# vim:ts=4:sts=4:syntax=python
