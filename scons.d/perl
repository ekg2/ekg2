
# perl

Import('*')

out = conf.PkgConfig(None, libs, ccflags, linkflags, None, 'perl', None,
	'-MExtUtils::Embed -e ccopts', '-MExtUtils::Embed -e ldopts')

Return('out')

# vim:ts=4:sts=4:sw=4:syntax=python
