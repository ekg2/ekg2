
# perl

Import('*')

out = conf.PkgConfig(None, flags, None, 'perl', None,
	'-MExtUtils::Embed -e "ccopts; ldopts"')

# XXX: do we need to test if we can build&link against perl?

Return('out')

# vim:ts=4:sts=4:sw=4:syntax=python
