
# jpeg (gg tokens)

Import('*')

out = conf.CheckLibWithHeader('jpeg', ['stdlib.h', 'stdio.h', 'jpeglib.h'], 'C', 'jpeg_start_decompress(NULL);', 0)
defines['HAVE_LIBJPEG'] = out
defines['HAVE_JPEGLIB_H'] = out
if (out):
	libs.append('jpeg')

Return('out')

# vim:ts=4:sts=4:sw=4:syntax=python
