
# libpcap (sniff)

Import('*')

out = conf.CheckLibWithHeader('pcap', ['pcap.h'], 'C', 'pcap_open_live(NULL, 0, 0, 0, NULL);', 0)
if (out):
	libs.append('pcap')

Return('out')

# vim:ts=4:sts=4:sw=4:syntax=python
