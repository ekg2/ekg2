
# inotify (xmsg)

Import('*')

out = conf.CheckHeader(['linux/soundcard.h'])
if not out:
	out = conf.CheckHeader(['sys/soundcard.h'])
	defines['HAVE_SYS_SOUNDCARD_H'] = out

Return('out')

# vim:ts=4:sts=4:sw=4:syntax=python
