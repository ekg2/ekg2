eval
	parametry: [kod]
	krotki opis: uruchom dowolny kod w ekg2
	
	Uruchom dowolny kod w jêzyku Python w ekg2. Kod ma dostêp modu³u ekg.
	Na przyk³ad spróbuj wykonaæ:
	
	  /python:eval ekg.echo("2 + 2 = %%s" %% (2 + 2))

list
	parametry:
	krotki opis: wypisz listê za³adowanych skryptów

load
	parametry: [skrypt]
	krotki opis: za³aduj skrypt jêzyka Python do ekg2 i uruchom go
	
	Za³aduj dowolny skrypt jêzyka Python do ekg2 i uruchom go. `skrypt` jest
	nazw± pliku w katalogu .ekg2/scripts lub .ekg2/<profile>/scripts.

run
	parametry: [skrypt]
	krotki opis: jednorazowo wykonaj skrypt jêzyka Python
	
	Jednorazowo wykonaj dowolny skrypt jêzyka Python. Zauwa¿, ¿e skrypt nie
	powinien rejestrowaæ ¿adnych handlerów - skrypt zostanie usuniêty z ekg2
	jak tylko skoñczy siê wykonywaæ. Zobacz równie¿ /python:load. `skrypt` jest
	nazw± pliku ze ¶cie¿k± wzglêdem CWD.

unload
	parametry: [skrypt]
	krotki opis: usuñ skrypt z ekg2
	
	Usuñ skrypt. To znaczy wyrejestruj wszystkie handlery i bindingi
	zarejestrowane przez dany skrypt i usuñ skrypt z pamiêci. `skrypt` jest
	nazw± skryptu, czyli nazw± pliku bez rozszerzenia '.py'.
