ctrld_quits
	type: bool
	default value: 1
	
	if set, Ctrl-D in status window terminates ekg2.

print_read_lines
	type: bool
	default value: 1
	
	if set to 1, all commands entered by user will be displayed in chat window among other messages.

